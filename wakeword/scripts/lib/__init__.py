"""Shared helpers for the Qube wake word training pipeline.

This package is intentionally dependency-light. The license gate (``licenses``)
uses only the standard library so it can run in CI without the heavy training
environment installed.
"""
