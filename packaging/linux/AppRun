#!/usr/bin/env bash
# AppImage entry point for the PyInstaller one-dir bundle.
set -euo pipefail

HERE="$(dirname "$(readlink -f "${0}")")"
export PATH="${HERE}/usr/bin/Qube:${PATH}"
exec "${HERE}/usr/bin/Qube/Qube" "$@"
