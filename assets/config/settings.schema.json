{
  "qube.engine.mode": {
    "type": "string",
    "enum": ["internal", "external"],
    "default": "internal",
    "description": "Inference backend: in-process GGUF (internal) or OpenAI-compatible localhost server (external)."
  },
  "qube.memory.enrichment": {
    "type": "boolean",
    "default": true,
    "description": "When enabled, extracts memories from chat, summarises sessions, and runs periodic memory reflection (higher RAM/LLM use). Retrieval of existing memories is unaffected when off."
  },
  "qube.native.modelPath": {
    "type": "string",
    "default": "",
    "description": "Path to the active internal .gguf model (shard 1 resolved automatically)."
  },
  "qube.native.gpuLayers": {
    "type": ["integer", "null"],
    "minimum": 0,
    "maximum": 200,
    "default": null,
    "description": "GPU layers for llama.cpp; null uses a hardware-based suggestion on first use."
  },
  "qube.native.cpuThreads": {
    "type": ["integer", "null"],
    "minimum": 1,
    "default": null,
    "description": "Blas/ggml thread count; null uses a CPU-based default on first use."
  },
  "qube.native.chatFormat": {
    "type": "string",
    "enum": ["auto", "jinja", "chatml", "llama-3", "mistral", "llama-2"],
    "default": "auto",
    "description": "Chat template selection for the internal llama.cpp engine."
  },
  "qube.native.promptLayout": {
    "type": "string",
    "enum": ["auto", "system_ok", "short_system", "flatten_user"],
    "default": "auto",
    "description": "Global prompt layout override for internal engine turns."
  },
  "qube.native.autoLoadOnStartup": {
    "type": "boolean",
    "default": false,
    "description": "Auto-load the saved internal model when entering internal mode or at startup."
  },
  "qube.native.reasoningDisplay": {
    "type": ["boolean", "null"],
    "default": null,
    "description": "Show thinking tokens in native chat; null means use model telemetry defaults."
  },
  "qube.models.directory": {
    "type": "string",
    "default": "",
    "description": "Directory for downloaded and native .gguf models; empty uses the app default models/llm folder."
  },
  "qube.modelManager.hardwareSuggestions": {
    "type": "boolean",
    "default": false,
    "description": "Rank and badge Qube Verified models by detected hardware in Model Manager."
  },
  "qube.onboarding.localLlmTourCompleted": {
    "type": "boolean",
    "default": false,
    "description": "Whether the first-run local LLM setup tour has been completed."
  },
  "qube.wakeword.activeId": {
    "type": "string",
    "default": "",
    "description": "Identifier of the active openWakeWord model."
  },
  "qube.wakeword.thresholds": {
    "type": "object",
    "default": {},
    "description": "Per-wakeword detection threshold overrides (wakeword id to float)."
  },
  "qube.audio.inputDeviceIndex": {
    "type": ["integer", "null"],
    "default": null,
    "description": "Preferred microphone device index; null uses the system default."
  },
  "qube.audio.outputDeviceIndex": {
    "type": ["integer", "null"],
    "default": null,
    "description": "Preferred speaker device index; null uses the system default."
  },
  "qube.notifications.enabled": {
    "type": "boolean",
    "default": true,
    "description": "Master switch for in-app and OS notifications."
  },
  "qube.notifications.dnd": {
    "type": "boolean",
    "default": false,
    "description": "Do Not Disturb — suppress non-critical notifications."
  },
  "qube.notifications.suppressWhenFocused": {
    "type": "boolean",
    "default": true,
    "description": "Suppress info/success toasts while the main window is focused."
  },
  "qube.notifications.soundEnabled": {
    "type": "boolean",
    "default": false,
    "description": "Play alert sounds for error/critical notifications."
  },
  "qube.notifications.osWhenHidden": {
    "type": "boolean",
    "default": true,
    "description": "Show OS notifications when the window is hidden or unfocused."
  },
  "qube.notifications.showPreview": {
    "type": "boolean",
    "default": false,
    "description": "Include message preview text in turn-complete notifications."
  },
  "qube.notifications.keepHistory": {
    "type": "boolean",
    "default": true,
    "description": "Persist recent notifications under ~/.qube/notification_history.json."
  },
  "qube.notifications.categories.voice": {
    "type": "boolean",
    "default": true,
    "description": "Voice pipeline notifications (STT failures, mic errors)."
  },
  "qube.notifications.categories.turnComplete": {
    "type": "boolean",
    "default": true,
    "description": "Notify when an assistant reply is ready while hidden."
  },
  "qube.notifications.categories.tools": {
    "type": "boolean",
    "default": true,
    "description": "Tool failure and action-required notifications."
  },
  "qube.notifications.categories.background": {
    "type": "boolean",
    "default": true,
    "description": "Background task completion (ingestion, etc.)."
  },
  "qube.notifications.categories.memory": {
    "type": "boolean",
    "default": false,
    "description": "Memory extraction and summary notifications."
  },
  "qube.notifications.categories.updates": {
    "type": "boolean",
    "default": true,
    "description": "App update and restart prompts."
  },
  "qube.companion.enabled": {
    "type": "boolean",
    "default": false,
    "description": "Show the floating desktop companion orb when Qube is tray-hidden."
  },
  "qube.companion.showWhenTrayHidden": {
    "type": "boolean",
    "default": true,
    "description": "Show companion while the main window is hidden to the system tray."
  },
  "qube.companion.showWhileWindowOpen": {
    "type": "boolean",
    "default": false,
    "description": "Keep companion visible even when the main window is open."
  },
  "qube.companion.autoHideIdle": {
    "type": "boolean",
    "default": true,
    "description": "Fade companion when idle."
  },
  "qube.companion.idleFadeSec": {
    "type": "integer",
    "minimum": 2,
    "maximum": 120,
    "default": 8,
    "description": "Seconds of idle listening before companion auto-fades."
  },
  "qube.companion.sizePx": {
    "type": "integer",
    "minimum": 48,
    "maximum": 80,
    "default": 56,
    "description": "Companion orb diameter in pixels."
  },
  "qube.companion.showCaption": {
    "type": "boolean",
    "default": false,
    "description": "Show a compact live caption chip below the orb."
  },
  "qube.companion.suppressOnFullscreen": {
    "type": "boolean",
    "default": true,
    "description": "Hide companion when a fullscreen app is detected (non-active turns)."
  },
  "qube.companion.tryOnWayland": {
    "type": "boolean",
    "default": false,
    "description": "Attempt floating overlay on Wayland (may be unreliable)."
  },
  "qube.companion.dockMode": {
    "type": "boolean",
    "default": false,
    "description": "Use edge-docked strip mode instead of floating orb (better on Wayland)."
  },
  "qube.companion.reducedMotion": {
    "type": ["boolean", "null"],
    "default": null,
    "description": "Override reduced motion; null follows system preference when detectable."
  },
  "qube.companion.position.x": {
    "type": ["integer", "null"],
    "default": null,
    "description": "Saved companion X coordinate."
  },
  "qube.companion.position.y": {
    "type": ["integer", "null"],
    "default": null,
    "description": "Saved companion Y coordinate."
  },
  "qube.companion.position.screen": {
    "type": "string",
    "default": "",
    "description": "QScreen.name() when position was saved."
  },
  "qube.companion.position.normX": {
    "type": ["number", "null"],
    "default": null,
    "description": "Normalized X (0-1) fallback when saved screen is missing."
  },
  "qube.companion.position.normY": {
    "type": ["number", "null"],
    "default": null,
    "description": "Normalized Y (0-1) fallback when saved screen is missing."
  },
  "qube.companion.position.dockEdge": {
    "type": "string",
    "enum": ["none", "left", "right", "bottom"],
    "default": "none",
    "description": "Edge dock preference for companion placement."
  }
}
