<!-- GENERATED by .cursor/hooks/starfall_export.py from roadmap.md + memory. Deterministic; do not hand-edit — re-run the exporter. -->

starfall implement Phase 3 (#61): Agent scope + egress summary, on branch `keith/mcp-capability-integration`.

Precondition: Phase 2 complete (see roadmap.md).
Read `.cursor/starfall/handoff.md`, `.cursor/starfall/active-task.md`, and `.cursor/starfall/drift-rules.md` first; refresh `active-task.md` for this phase.

Objectives:
Agent scope + egress summary

Carried-forward known issues:
- KI2 `_adapter` overloaded with full URN string
- KI4 Preset bundle partial-deny UX opaque

Constraints (must hold):
- MCP is a provider only; no raw MCP tools exposed as primary UX.

- No `import mcp` / `provider == "mcp"` outside `providers/mcp/` (P6).

- Preserve P1-P8; every result carries `cap:` provenance (P4/P8).

- Nothing defaults to write/destructive; unknown classification is default-deny + review (P7).

Workflow: Discovery -> Architecture Review -> Implementation Plan -> Implementation Handoff -> Code -> Self-Review -> Testing -> Documentation -> Closing.
Gates G1-G3 before Code; G4 before close. Never commit/push to main/master.
Close only when G1-G4 PASS and handoff STATUS: READY (evidence-verified by starfall_verify).
